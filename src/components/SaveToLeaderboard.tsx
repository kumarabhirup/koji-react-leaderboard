/**
 * @class SaveToLeaderboard
 */

import * as React from 'react'

type Props = { text: string }

export class SaveToLeaderboard extends React.Component<Props> {
  render(): React.ReactNode {
    return <div>SaveLeaderboard</div>
  }
}
